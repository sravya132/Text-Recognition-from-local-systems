services:
  - type: worker
    name: ocr-app
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: python ocr_basic.py
